    运行顺序：
    python scripts/train_model.py → 训练模型。
    python scripts/generate_annotations.py → 生成初始 JSON。
    人工编辑 outputs/annotations.json 填写备注信息。
    python scripts/record_script.py → 录制脚本。
    python scripts/replay_script.py → 回放脚本。
    python scripts/generate_report.py → 生成报告。

